{% trans_default_domain 'sfs_cms_contents' %}

{% extends '@SfsCms/admin_layout.html.twig' %}

{% form_theme form
    'bootstrap_5_layout.html.twig'
    '@SfsCms/forms/routes_theme.html.twig'
    '@SfsCms/forms/types_theme.html.twig'
    '@SfsCmsTranslationPlugin/forms/translation_form_theme.bootstrap5.html.twig' %}

{% block title %}{{ ('admin_'~content_type~'.translations.meta.title')|trans({'%name%':content_entity.name}) }}{% endblock %}

{% block breadcrums_content %}
    {{ parent() }}
    <li class="breadcrumb-item"><a
                href="{{ url('sfs_cms_admin_content_'~content_type~'_list') }}">{{ ('admin_'~content_type~'.list.breadcrumb')|trans }}</a>
    </li>
    <li class="breadcrumb-item"><a
                href="{{ url('sfs_cms_admin_content_'~content_type~'_details', {'content':entity}) }}">{{ ('admin_'~content_type~'.read.breadcrumb')|trans({'%name%': entity.name}) }}</a>
    </li>
    <li class="breadcrumb-item active"
        aria-current="content">{{ ('admin_'~content_type~'.translations.breadcrumb')|trans({'%name%':content_entity.name}) }}</li>
{% endblock breadcrums_content %}

{% block content %}
    {% include ['@content/'~content_type~'/admin/_content_title.html.twig', '@SfsCms/admin/content/_content_title.html.twig']  with {'current':'translations', 'content_type':content_type, 'content_entity':entity, 'version_entity': prev_version|default(false)} %}
    {% include ['@content/'~content_type~'/admin/_content_tabs.html.twig', '@SfsCms/admin/content/_content_tabs.html.twig']  with {'current':'translations', 'content':content_entity, 'entity':version_entity} %}

    <div class="container-fluid mb-3">
        <div class="row">
            <div class="col">
                <div class="float-end">
                    {% if not content_config.admin.version_translations_import.is_granted or is_granted(content_config.admin.version_translations_import.is_granted, entity) %}
                        <a class="btn btn-outline-secondary"
                           href="{{ url('sfs_cms_admin_content_'~content_type~'_translations_import', {'content':entity, 'back':'translations'}) }}">
                            <i class="bi bi-upload"></i>
                            {{ ('admin_'~content_type~'.translations.actions.import_translations.link')|trans }}
                        </a>
                    {% else %}
                        <a class="btn btn-outline-secondary disabled">
                            <i class="bi bi-upload"></i>
                            {{ ('admin_'~content_type~'.translations.actions.import_translations.link')|trans }}
                        </a>
                    {% endif %}

                    {% if not content_config.admin.version_translations_export.is_granted or is_granted(content_config.admin.version_translations_export.is_granted, entity) %}
                        {% if content_config.admin.version_translations_export.formats|length > 1 %}
                            <div class="btn-group">
                                <button type="button" class="btn btn-outline-secondary dropdown-toggle" title="Export"
                                        data-bs-toggle="dropdown" aria-expanded="false">
                                    <i class="bi bi-download"></i>
                                    {{ ('admin_'~content_type~'.translations.actions.export_translations.link')|trans }}
                                </button>
                                <ul class="dropdown-menu dropdown-menu-end">
                                    {% for format in content_config.admin.version_translations_export.formats %}
                                        <li><a class="dropdown-item"
                                               href="{{ url('sfs_cms_admin_content_'~content_type~'_translations_export_all', {'content':entity, 'back':'translations', 'format': format}) }}">{{ ('admin_page.translations.export.formats.'~format)|trans }}</a>
                                        </li>
                                    {% endfor %}
                                </ul>
                            </div>
                        {% else %}
                            {% set format = content_config.admin.version_translations_export.formats|first %}
                            <a class="btn btn-outline-secondary"
                               href="{{ url('sfs_cms_admin_content_'~content_type~'_translations_export_all', {'content':entity, 'back':'translations', 'format': format}) }}"
                               title="{{ ('admin_page.translations.export.formats.'~format)|trans }}">
                                <i class="bi bi-download"></i>
                                {{ ('admin_'~content_type~'.translations.actions.export_translations.link')|trans }}
                            </a>
                        {% endif %}
                    {% else %}
                        <a class="btn btn-outline-secondary disabled">
                            <i class="bi bi-download"></i>
                            {{ ('admin_'~content_type~'.translations.actions.export_translations.link')|trans }}
                        </a>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>

    <div class="container-fluid bg-white">
        <div class="row">
            <div class="col pb-3">

                {% if app.request.attributes.has('_translations_imported') %}
                    <div class="ui green message" role="alert">
                        Translations imported successfully, review them and <strong>save a new version to apply
                            changes</strong>.
                    </div>
                {% endif %}

                {% for warning in importWarnings|default %}
                    <div class="ui orange message" role="alert">
                        {{ warning }}
                    </div>
                {% endfor %}


                {#                {% set colors = [ 'primary', 'secondary', 'info', 'success', 'warning', 'danger' ] %} #}
                {{ form_start(form) }}
                <table class="table">
                    <tr>
                        <th style="width: 10%">&nbsp;</th>
                        {% for i, locale in content_entity.locales %}
                            {% set color = (loop.first ? 'primary' : 'secondary') %}
                            {% set currentLocale = locale %}
                            <th style="height: 50px" {{ loop.first ? 'data-default-locale-trans='~locale : 'data-locale-trans='~locale }}
                                class="{{ loop.index > 2 ? 'd-none hidden' :'' }}">
                                <div class="d-flex align-items-center justify-content-between h-100 bg-{{ color }} p-2 bg-opacity-10 text-center p-3 text-{{ colors[i]|default('dark') }}">

                                    {% if i > 0 %}
                                        <button class="p-1 text-primary bg-white border border-1 rounded" type="button"
                                                data-translate-all-target-locale="{{ locale }}">
                                            {% if sfs_cms_translation.api.driver|default('none') == 'google' %}
                                                <img src="https://upload.wikimedia.org/wikipedia/commons/d/d7/Google_Translate_logo.svg"
                                                     width="16" height="18">
                                            {% endif %}
                                        </button>
                                    {% else %}
                                        <span>&nbsp;</span>
                                    {% endif %}

                                    {% if content_entity.locales|length > 2 and not loop.first %}
                                        <div class="btn-group">
                                            <button class="btn btn-link dropdown-toggle px-2 text-dark" type="button"
                                                    data-bs-toggle="dropdown" aria-expanded="false">
                                                <i class="bi bi-translate"></i> {{ locale }}
                                            </button>
                                            <ul class="dropdown-menu">
                                                {% for i, locale in content_entity.locales %}
                                                    {% if locale != currentLocale and  not loop.first %}
                                                        <li>
                                                            <button type="button" class="dropdown-item px-2"
                                                                    data-locale-trans-toggler="{{ locale }}"><i
                                                                        class="bi bi-translate"></i> {{ locale }}
                                                            </button>
                                                        </li>
                                                    {% endif %}
                                                {% endfor %}
                                            </ul>
                                        </div>
                                    {% else %}
                                        <span>
                                            <i class="bi bi-translate"></i>
                                            {{ locale }}
                                        </span>
                                    {% endif %}

                                    {% if not content_config.admin.version_translations_export.is_granted or is_granted(content_config.admin.version_translations_export.is_granted, entity) %}
                                        {% if content_config.admin.version_translations_export.formats|length > 1 %}
                                            <div class="btn-group">
                                                <button type="button" class="btn btn-link dropdown-toggle"
                                                        title="Export"
                                                        data-bs-toggle="dropdown" aria-expanded="false">
                                                    <i class="bi bi-download"></i>
                                                </button>
                                                <ul class="dropdown-menu dropdown-menu-end">
                                                    {% for format in content_config.admin.version_translations_export.formats %}
                                                        <li><a class="dropdown-item"
                                                               href="{{ url('sfs_cms_admin_content_'~content_type~'_translations_export', {'content':entity, 'back':'translations', 'target':locale, 'format': format}) }}">{{ ('admin_page.translations.export.formats.'~format)|trans }}</a>
                                                        </li>
                                                    {% endfor %}
                                                </ul>
                                            </div>
                                        {% else %}
                                            {% set format = content_config.admin.version_translations_export.formats|first %}
                                            <a class="btn btn-link float-end p-0"
                                               href="{{ url('sfs_cms_admin_content_'~content_type~'_translations_export', {'content':entity, 'back':'translations', 'target':locale, 'format': format}) }}"
                                               title="{{ ('admin_page.translations.export.formats.'~format)|trans }}">
                                                <i class="bi bi-download"></i>
                                            </a>
                                        {% endif %}
                                    {% endif %}
                                </div>
                            </th>
                        {% endfor %}
                    </tr>

                    {% set module_type = '' %}
                    {% set module = '' %}
                    {% for field in form|filter( (field) => field.vars.attr['data-module']|default(false) ) %}
                        {% if field.vars.attr['data-module-type'] != module_type or field.vars.attr['data-module'] != module %}
                            {% set module_type = field.vars.attr['data-module-type'] %}
                            {% set module = field.vars.attr['data-module'] %}
                            <tr>
                                <td colspan="50" class="py-3 bg-light">
                                    <strong> {{ field.vars.attr['data-module']|split(':')|join(' > ') }}
                                        {% if module_type != 'seo' %}
                                            &gt; {{ (module_type~'.label')|trans({}, 'sfs_cms_modules') }}
                                        {% endif %}
                                    </strong>
                                </td>
                            </tr>
                        {% endif %}

                        <tr>
                            {# <td>{{ field.vars.name }}</td> #}
                            <td>
                                {% if module_type == 'seo' %}
                                    {{ ('admin_page.form.seo.'~field.vars.attr['data-module-field']~'.label')|trans({}, 'sfs_cms_contents') }}
                                {% else %}
                                    {{ (module_type~'.form.'~field.vars.attr['data-module-field']~'.label')|trans({}, 'sfs_cms_modules') }}
                                {% endif %}
                                {{ form_widget(field._trans_id) }}
                                {{ form_widget(field._default) }}
                            </td>

                            {% for locale in content_entity.locales %}
                                <td {{ loop.first ? 'data-default-locale-trans='~locale : 'data-locale-trans='~locale }}
                                        class="{{ loop.index > 2 ? 'd-none hidden' :'' }}">
                                    {{ form_widget(field.offsetGet(locale)) }}
                                </td>
                            {% endfor %}
                        </tr>
                    {% endfor %}
                </table>

                {{ form_rest(form) }}

                <div class="d-flex justify-content-end align-items-center">
                    <a href="{{ url('sfs_cms_admin_content_'~content_type~'_details', {'content':entity}) }}"
                       class="btn btn-outline-secondary">{{ ('admin_'~content_type~'.translations.actions.cancel.button')|trans }}</a>
                    <input type="submit" value="{{ ('admin_'~content_type~'.translations.actions.save.button')|trans }}"
                           class="btn btn-primary ms-3"/>
                </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock content %}
